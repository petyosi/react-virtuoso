---
title: 'Tutorial 02: Columns, Cells, and Headers'
description: Define visible columns declaratively and render custom cells and headers for the shared products dataset.
sidebar:
  order: 2
  label: "2.Columns, Cells, Headers"
---

The basic mental model is:

- `DataTable` owns scrolling and virtualization
- each `DataTableColumn` declares one field
- `DataTableColumnHeader` renders the sticky header cell
- `DataTableCell` renders a visible row cell for that field

## Product table foundations

```tsx live wide file=App.tsx
import { DataTable, DataTableCell, DataTableColumn, DataTableColumnHeader } from '@/components/ui/data-table'

import { products } from './data'

const currency = new Intl.NumberFormat('en-US', {
  currency: 'USD',
  style: 'currency',
})

export default function App() {
  return (
    <DataTable className="rounded-xl" data={{ data: products, groups: [] }} style={{ height: 380 }}>
      <DataTableColumn field="name">
        <DataTableColumnHeader>Product</DataTableColumnHeader>
        <DataTableCell>
          {({ row }) => (
            <div className="flex flex-col">
              <span className="font-medium">{row.data.name}</span>
              <span className="text-xs text-muted-foreground">{row.data.id}</span>
            </div>
          )}
        </DataTableCell>
      </DataTableColumn>

      <DataTableColumn field="category">
        <DataTableColumnHeader>Category</DataTableColumnHeader>
        <DataTableCell>{({ cellValue }) => String(cellValue)}</DataTableCell>
      </DataTableColumn>

      <DataTableColumn field="rating">
        <DataTableColumnHeader>Rating</DataTableColumnHeader>
        <DataTableCell className="font-medium text-amber-600">{({ cellValue }) => Number(cellValue).toFixed(1) + ' / 5'}</DataTableCell>
      </DataTableColumn>

      <DataTableColumn field="price">
        <DataTableColumnHeader className="justify-end">Price</DataTableColumnHeader>
        <DataTableCell className="text-right tabular-nums">{({ cellValue }) => currency.format(Number(cellValue))}</DataTableCell>
      </DataTableColumn>

      <DataTableColumn field="stock">
        <DataTableColumnHeader className="justify-end">Stock</DataTableColumnHeader>
        <DataTableCell className="text-right tabular-nums">
          {({ row }) => <span className={row.data.stock < 16 ? 'text-amber-600' : 'text-foreground'}>{row.data.stock}</span>}
        </DataTableCell>
      </DataTableColumn>
    </DataTable>
  )
}
```

```ts live file=data.ts
export interface Product {
  id: string
  name: string
  category: string
  price: number
  stock: number
  rating: number
}

const categories = ['Office', 'Peripherals', 'Audio', 'Software']

export const products: Product[] = Array.from({ length: 120 }, (_, index) => ({
  id: `SKU-${String(index + 1).padStart(3, '0')}`,
  name: `${categories[index % categories.length]} Item ${index + 1}`,
  category: categories[index % categories.length]!,
  price: 49 + (index % 11) * 13,
  stock: (index * 7) % 48,
  rating: 3 + (index % 20) / 10,
}))
```

## Takeaways

- Keep each column focused on one field so rendering stays predictable.
- Use the full `row.data` object when a cell needs richer formatting than the raw field value.
- Right-align numeric columns early, because later features like sticky columns and grouping work better when the table already has consistent alignment rules.
