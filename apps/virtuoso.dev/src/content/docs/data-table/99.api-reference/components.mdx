---
title: 'Components'
sidebar:
  order: 99
  label: 'Components'
---

## Cell

> **Cell**(`__namedParameters`): `null`

Declares the cell renderer for the current column.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`Props`](../components/#props) |

### Returns

`null`


---

## CellRenderFunction

> **CellRenderFunction** = (`params`) => `ReactNode`

The render function used by a cell definition.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `params` | [`CellRenderParams`](../components/#cellrenderparams) |

### Returns

`ReactNode`


---

## CellRenderParams

The parameters passed to a cell renderer.

### Properties

#### cellValue

`unknown`

***

#### column

[`ColumnInfo`](../components/#columninfo)

***

#### columnKey

`string`

***

#### columnState

[`ColumnState`](../state/#columnstate)

***

#### overlaidByScrollbar

`boolean`

***

#### row

[`Row`](../misc/#row)\<`unknown`\>


---

## Column

> **Column**(`__namedParameters`): `Element`

Declares a visible column in the table.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`Props`](../components/#props) |

### Returns

`Element`


---

## ColumnGroup

> **ColumnGroup**(`__namedParameters`): `Element`

Groups related columns under a shared header.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | [`Props`](../components/#props) |

### Returns

`Element`


---

## ColumnGroupHeader

> **ColumnGroupHeader**(`props`): `null`

Declares the header renderer for the current column group.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | [`Props`](../components/#props) |

### Returns

`null`


---

## ColumnGroupHeaderCustomComponent

> **ColumnGroupHeaderCustomComponent** = `React.ComponentType`\<[`ColumnGroupHeaderRenderParams`](../components/#columngroupheaderrenderparams)\>

A React component variant for a column group header.


---

## ColumnGroupHeaderRenderFunction

> **ColumnGroupHeaderRenderFunction** = (`params`) => `ReactNode`

The render function used by a column group header.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `params` | [`ColumnGroupHeaderRenderParams`](../components/#columngroupheaderrenderparams) |

### Returns

`ReactNode`


---

## ColumnGroupHeaderRenderParams

The parameters passed to a column group header renderer.

### Properties

#### columnKeys

`string`[]

***

#### group

[`ColumnGroupInfo`](../components/#columngroupinfo)

***

#### groupId

`string`

***

#### totalWidth

`number`


---

## ColumnGroupInfo

Describes a registered column group.

### Properties

#### childColumnIds

`string`[]

***

#### childGroupIds

`string`[]

***

#### depth

`number`

***

#### id

`string`

***

#### parentGroupId?: `string`


***

#### sticky?: `"left"` \| `"right"`



---

## ColumnHeader

> **ColumnHeader**(`props`): `null`

Declares the header renderer for the current column.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | [`Props`](../components/#props) |

### Returns

`null`


---

## ColumnHeaderCustomComponent

> **ColumnHeaderCustomComponent** = `React.ComponentType`\<[`ColumnHeaderRenderParams`](../components/#columnheaderrenderparams)\>

A React component variant for a column header.


---

## ColumnHeaderRenderFunction

> **ColumnHeaderRenderFunction** = (`params`) => `ReactNode`

The render function used by a column header.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `params` | [`ColumnHeaderRenderParams`](../components/#columnheaderrenderparams) |

### Returns

`ReactNode`


---

## ColumnHeaderRenderParams

The parameters passed to a column header renderer.

### Properties

#### column

[`ColumnInfo`](../components/#columninfo)

***

#### columnKey

`string`

***

#### columnState

[`ColumnState`](../state/#columnstate)

***

#### overlaidByScrollbar

`boolean`


---

## ColumnInfo

Describes a registered column in the table.

### Extended by

- [`Props`](../components/#props)

### Properties

#### field

`string`

***

#### groupId?: `string`


***

#### sticky?: `"left"` \| `"right"`



---

## GroupHeaderCell

> **GroupHeaderCell**(`props`): `null`

Declares the renderer used for group header rows.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | [`Props`](../components/#props) |

### Returns

`null`


---

## GroupHeaderCustomComponent

> **GroupHeaderCustomComponent** = `React.ComponentType`\<[`GroupHeaderRenderParams`](../components/#groupheaderrenderparams)\>

A React component variant for a group header row.


---

## GroupHeaderRenderFunction

> **GroupHeaderRenderFunction** = (`params`) => `ReactNode`

The render function used by a group header row.

### Parameters

| Parameter | Type |
| ------ | ------ |
| `params` | [`GroupHeaderRenderParams`](../components/#groupheaderrenderparams) |

### Returns

`ReactNode`


---

## GroupHeaderRenderParams

The parameters passed to a group header renderer.

### Properties

#### level

`number`

***

#### row

[`Row`](../misc/#row)\<`unknown`\>


---

## Props

The properties accepted by the `Cell` component.

### Properties

#### children

[`CellRenderFunction`](../components/#cellrenderfunction)

***

#### className?: `string`



---

## Props

The properties accepted by the `Column` component.

### Extends

- [`ColumnInfo`](../components/#columninfo)

### Properties

#### children?: `ReactNode`


***

#### field

`string`

##### Inherited from

[`ColumnInfo`](../components/#columninfo).[`field`](../components/#field)

***

#### groupId?: `string`


##### Inherited from

[`ColumnInfo`](../components/#columninfo).[`groupId`](../components/#groupid)

***

#### sticky?: `"left"` \| `"right"`


##### Overrides

[`ColumnInfo`](../components/#columninfo).[`sticky`](../components/#sticky)


---

## Props

The properties accepted by the `ColumnGroup` component.

### Properties

#### children?: `ReactNode`


***

#### sticky?: `"left"` \| `"right"`



---

## Props

> **Props** = \{ `children`: [`ColumnGroupHeaderRenderFunction`](../components/#columngroupheaderrenderfunction); `className?`: `string`; \} \| \{ `className?`: `string`; `component`: [`ColumnGroupHeaderCustomComponent`](../components/#columngroupheadercustomcomponent); \}

The properties accepted by the `ColumnGroupHeader` component.


---

## Props

> **Props** = \{ `children`: [`ColumnHeaderRenderFunction`](../components/#columnheaderrenderfunction); `className?`: `string`; \} \| \{ `className?`: `string`; `component`: [`ColumnHeaderCustomComponent`](../components/#columnheadercustomcomponent); \}

The properties accepted by the `ColumnHeader` component.


---

## Props

> **Props** = \{ `children`: [`GroupHeaderRenderFunction`](../components/#groupheaderrenderfunction); `className?`: `string`; \} \| \{ `className?`: `string`; `component`: [`GroupHeaderCustomComponent`](../components/#groupheadercustomcomponent); \}

The properties accepted by the `GroupHeaderCell` component.


---

## ScrollerProps

> **ScrollerProps** = `Omit`\<`React.HTMLProps`\<`HTMLDivElement`\>, `"ref"` \| `"data"` \| `"onScroll"`\>

The DOM attributes that you can pass to the `VirtuosoDataTable` component to customize the scroll element.


---

## VirtuosoDataTable

> **VirtuosoDataTable**\<`Data`, `Context`, `Group`\>(`props`): `ReactElement`

The React component that renders the data table. Refer to [VirtuosoDataTableProps](../components/#virtuosodatatableprops) for the accepted props.
The component accepts a ref that can be used to call methods on the data table. See [VirtuosoDataTableMethods](../imperative-api/#virtuosodatatablemethods) for the available methods.

### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `Data` | - |
| `Context` | - |
| `Group` | `unknown` |

### Parameters

| Parameter | Type |
| ------ | ------ |
| `props` | [`VirtuosoDataTableProps`](../components/#virtuosodatatableprops)\<`Data`, `Context`, `Group`\> & `object` |

### Returns

`ReactElement`


---

## VirtuosoDataTableProps

The properties accepted by the `VirtuosoDataTable` component.
In addition to the properties listed here, you can pass any other props that are accepted by a `div` element. They will be passed to the root div element used for the scroll.

### Extends

- [`ScrollerProps`](../components/#scrollerprops)

### Type Parameters

| Type Parameter | Default type | Description |
| ------ | ------ | ------ |
| `Data` | - | The type of the data items in the table. |
| `Context` | - | The type of the context passed to the table. |
| `Group` | `unknown` | - |

### Properties

#### children?: `ReactNode`


Any children passed to the component.

##### Overrides

`ScrollerProps.children`

***

#### columnOverscanCount?: `number`


The number of extra columns to render on each side of the visible columns. Useful for smooth horizontal scrolling.
Defaults to 0 (no overscan).

***

#### computeRowKey?

> `optional` **computeRowKey**: `NoInfer`\<(`params`) => `Key`\>

Computes the value for the React `key` prop for the item at the specified index. Use stable, unique keys to avoid rendering issues.

##### Param

The parameters to compute the key.

***

#### context?: `Context`


Any additional state that you need to use in the `ItemContent`, `EmptyPlaceholder`, etc.

***

#### customScrollParent?: `HTMLElement` \| `null`


Pass an HTML element to use as the scroll element. This is useful when you want to include additional content in the scrollable area, or if you want to use a complex custom scroll component.

***

#### data?: [`TableData`](../misc/#tabledata)\<`Data`, `Group`\>


The data to display in the table.

***

#### EmptyPlaceholder?: `NoInfer`\<[`ContextAwareComponent`](../customization/#contextawarecomponent)\<`Context`\>\>


An optional React component to render when the table has zero data items.

***

#### engineId?: `string`


Optional stable engine ID for remote access via `useRemote*` hooks.
When provided, sibling or ancestor components can read from and publish to the table engine using this ID.

***

#### engineRef?: [`EngineRef`](../misc/#engineref)


Optional reactive ref to expose the table engine instance.
Create it with `useEngineRef()` and use it with `useRemote*` hooks from outside the table tree.

***

#### increaseViewportBy?: `number`


Artificially extends the viewport size in both directions, causing more items to be rendered.
Useful if you have relatively heavy items or want images to be loaded before the user scrolls to them.
For example, setting the value to `100` would increase the viewport in both directions by `100` pixels, causing more items to be rendered.

***

#### initialLocation?: [`RowLocation`](../scroll-location/#rowlocation)


The initial location to scroll to. It will be applied the first time the table is rendered with data.
Using this property allows you to skip rendering of the items at the top of the table.

***

#### model?: [`DataModelHandle`](../data-models/#datamodelhandle)\<`Data` \| `Group`\>


An optional data model handle that provides data to the table through a message-exchange protocol.
When provided, the `data` prop is ignored and data flows through the model bridge instead.

***

#### onRenderedDataChange()?

> `optional` **onRenderedDataChange**: (`range`) => `void`

A callback that's invoked when the currently visible items change

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `range` | `Data`[] |

##### Returns

`void`

***

#### onScroll()?

> `optional` **onScroll**: (`location`) => `void`

A callback that's invoked when the table is scrolled. See [ListScrollLocation](../state/#listscrolllocation) for the details of the parameter received.

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `location` | [`ListScrollLocation`](../state/#listscrolllocation) |

##### Returns

`void`

***

#### ScrollElement?: `NoInfer`\<[`ScrollElementComponent`](../customization/#scrollelementcomponent)\<`Context`\>\>


An optional React component to replace the default scroller element. The default value is a `div` element.

***

#### useWindowScroll?: `boolean`


Set to true to make the table use the document scroller rather than wrapping in its own.

