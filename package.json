{
  "name": "@virtuoso.dev/monorepo",
  "private": true,
  "scripts": {
    "ci": "pnpm ci-setup && pnpm format:check && pnpm build && pnpm typecheck && pnpm lint && pnpm lint:md && pnpm test && pnpm e2e",
    "ci-setup": "pnpm run --if-present --parallel --no-bail ci-setup",
    "typecheck": "pnpm run --if-present --parallel typecheck",
    "lint": "pnpm run --if-present --parallel --no-bail lint",
    "lint:md": "markdownlint '**/*.md' --ignore-path .markdownlintignore",
    "lint:md:fix": "markdownlint '**/*.md' --ignore-path .markdownlintignore --fix",
    "format": "oxfmt && pnpm --filter @virtuoso.dev/virtuoso.dev exec prettier --write '**/*.astro'",
    "format:check": "oxfmt --check && pnpm --filter @virtuoso.dev/virtuoso.dev exec prettier --check '**/*.astro'",
    "test": "pnpm run --if-present --parallel --no-bail test",
    "e2e": "pnpm run --if-present --parallel --no-bail e2e",
    "build": "pnpm --if-present --recursive run build",
    "build:skills": "pnpm --filter @virtuoso.dev/virtuoso-skills build",
    "validate:skills": "claude plugin validate . --strict && pnpm --filter @virtuoso.dev/virtuoso-skills validate",
    "changeset-version": "changeset version && pnpm --filter @virtuoso.dev/virtuoso-skills sync-version",
    "release": "pnpm build && pnpm exec changeset publish",
    "changeset-add": "pnpm changeset add",
    "dev:docs": "pnpm --filter @virtuoso.dev/virtuoso.dev run dev",
    "build-virtuoso.dev": "pnpm --filter @virtuoso.dev/virtuoso.dev run build",
    "prepare": "lefthook install",
    "knip": "knip"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "@types/node": "catalog:",
    "@typescript/native-preview": "7.0.0-dev.20260301.1",
    "knip": "catalog:",
    "lefthook": "^2.0.8",
    "markdownlint-cli": "^0.48.0",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": ">=10.24",
      "onFail": "error"
    },
    "runtime": {
      "name": "node",
      "version": ">=22.0.0",
      "onFail": "error"
    }
  },
  "packageManager": "pnpm@10.24.0",
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "react-inspector>react": "19"
      }
    }
  }
}
