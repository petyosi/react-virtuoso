---
'@virtuoso.dev/reactive-engine-core': patch
---

Add an activation-scoped `Pulsar` node for valueless scheduled emissions from readable cadence state. Disposing a child engine now also removes subscriptions that it delegated to a parent-owned node.
