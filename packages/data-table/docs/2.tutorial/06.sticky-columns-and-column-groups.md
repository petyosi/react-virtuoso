---
title: 'Tutorial 06: Sticky Columns and Column Groups'
description: Keep key identifiers pinned while organizing related metrics under grouped headers.
sidebar:
  label: Sticky Columns and Groups
---

Wide operational tables usually need two structural tools at the same time:

- sticky columns to keep identity and actions visible
- column groups to show how related fields belong together

## Pinned identity with grouped metrics

```tsx live wide file=App.tsx
import { ColumnGroup, ColumnGroupHeader } from '@/components/ui/data-table'
import { DataTable, DataTableCell, DataTableColumn, DataTableColumnHeader } from '@/components/ui/data-table'

import { products } from './data'

const currency = new Intl.NumberFormat('en-US', {
  currency: 'USD',
  style: 'currency',
})

export default function App() {
  return (
    <DataTable className="rounded-xl" columnOverscanCount={1} data={{ data: products, groups: [] }} style={{ height: 420 }}>
      <DataTableColumn field="name" sticky="left">
        <DataTableColumnHeader>Product</DataTableColumnHeader>
        <DataTableCell className="font-medium">{({ row }) => row.data.name}</DataTableCell>
      </DataTableColumn>

      <DataTableColumn field="vendor" sticky="left">
        <DataTableColumnHeader>Vendor</DataTableColumnHeader>
        <DataTableCell>{({ cellValue }) => String(cellValue)}</DataTableCell>
      </DataTableColumn>

      <ColumnGroup>
        <ColumnGroupHeader className="px-2 text-center text-xs font-semibold uppercase tracking-wide text-muted-foreground">
          {() => 'Inventory'}
        </ColumnGroupHeader>

        <DataTableColumn field="stock">
          <DataTableColumnHeader className="justify-end">Stock</DataTableColumnHeader>
          <DataTableCell className="text-right tabular-nums">{({ cellValue }) => String(cellValue)}</DataTableCell>
        </DataTableColumn>

        <DataTableColumn field="status">
          <DataTableColumnHeader>Status</DataTableColumnHeader>
          <DataTableCell>{({ cellValue }) => String(cellValue)}</DataTableCell>
        </DataTableColumn>
      </ColumnGroup>

      <ColumnGroup>
        <ColumnGroupHeader className="px-2 text-center text-xs font-semibold uppercase tracking-wide text-muted-foreground">
          {() => 'Commercial'}
        </ColumnGroupHeader>

        <DataTableColumn field="price">
          <DataTableColumnHeader className="justify-end">Price</DataTableColumnHeader>
          <DataTableCell className="text-right tabular-nums">{({ cellValue }) => currency.format(Number(cellValue))}</DataTableCell>
        </DataTableColumn>

        <DataTableColumn field="revenue">
          <DataTableColumnHeader className="justify-end">Revenue</DataTableColumnHeader>
          <DataTableCell className="text-right tabular-nums">{({ cellValue }) => currency.format(Number(cellValue))}</DataTableCell>
        </DataTableColumn>
      </ColumnGroup>

      <DataTableColumn field="region" sticky="right">
        <DataTableColumnHeader>Region</DataTableColumnHeader>
        <DataTableCell>{({ cellValue }) => String(cellValue)}</DataTableCell>
      </DataTableColumn>
    </DataTable>
  )
}
```

```ts live file=data.ts
export const products = Array.from({ length: 120 }, (_, index) => {
  const stock = (index * 5) % 32

  return {
    id: `SKU-${String(index + 1).padStart(3, '0')}`,
    name: `Product ${index + 1}`,
    vendor: ['Northwind', 'Acme', 'Orbit'][index % 3]!,
    stock,
    status: stock < 6 ? 'Backorder' : stock < 12 ? 'Low stock' : 'In stock',
    price: 49 + (index % 11) * 13,
    revenue: 1_100 + index * 85,
    region: ['US', 'EU', 'APAC'][index % 3]!,
  }
})
```

## Notes

- Pin the smallest high-value identifiers, not every important field.
- Group headers are especially helpful when horizontal scrolling introduces metrics that would otherwise feel detached from each other.
- `columnOverscanCount` helps wide tables feel smoother during sideways scrolling.
