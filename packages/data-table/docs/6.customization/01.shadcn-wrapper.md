---
title: The Editable shadcn Wrapper
description: Understand what the registry wrapper provides and how to customize it safely.
sidebar:
  label: Editable Wrapper
---

The shadcn registry copies a wrapper into `@/components/ui/data-table` that re-exports the headless engine from `@virtuoso.dev/data-table` with default styling on top. The wrapper is your code — edit it like any other app file.

```tsx live
import { DataTable, DataTableCell, DataTableColumn, DataTableColumnHeader } from '@/components/ui/data-table'
import { localModel } from '@virtuoso.dev/data-table'

const rows = [
  { product: 'Standing Desk', owner: 'Avery', status: 'Launch ready' },
  { product: 'USB-C Dock', owner: 'Morgan', status: 'In review' },
  { product: 'Mechanical Keyboard', owner: 'Riley', status: 'Backorder' },
]

const model = localModel({ data: rows })

export default function App() {
  return (
    <DataTable className="rounded-xl border-2 border-border/70 shadow-sm" model={model} style={{ height: 280 }}>
      <DataTableColumn field="product">
        <DataTableColumnHeader>Product</DataTableColumnHeader>
        <DataTableCell className="font-medium">{({ cellValue }) => String(cellValue)}</DataTableCell>
      </DataTableColumn>
      <DataTableColumn field="owner">
        <DataTableColumnHeader>Owner</DataTableColumnHeader>
        <DataTableCell>{({ cellValue }) => String(cellValue)}</DataTableCell>
      </DataTableColumn>
      <DataTableColumn field="status">
        <DataTableColumnHeader>Status</DataTableColumnHeader>
        <DataTableCell>
          {({ cellValue }) => <span className="rounded-full bg-muted px-2 py-1 text-xs font-medium">{String(cellValue)}</span>}
        </DataTableCell>
      </DataTableColumn>
    </DataTable>
  )
}
```

The wrapper covers presentation: default row, header, sticky-column, empty, and loading components; Tailwind classes for cells, headers, and group headers; ergonomic names (`DataTable`, `DataTableColumn`, `DataTableColumnHeader`, `DataTableCell`); and re-exports for commonly-used headless hooks.

The headless package covers behavior: virtualization, measurement, scrolling, `localModel()` and `remoteModel()`, the reactive engine, and opt-in feature modules (resize, reorder, visibility, persistence).

Behavior imports always come from `@virtuoso.dev/data-table` so your styling edits never fork table mechanics.

Feature-specific shadcn components follow the same split:

- `data-table-resize-handle` — resize handle UI
- `data-table-reorder-grip`, `data-table-reorder-drop-zone`, `data-table-draggable-group-header` — reorder UI

These are header-slot UI. The streams they publish to live in the corresponding `@virtuoso.dev/data-table/...` subpaths.
