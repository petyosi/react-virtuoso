# STRICT MODE: Enforce proper dependency declarations
# This will surface phantom dependencies that need to be fixed
shamefully-hoist=false

# Strict peer dependencies - enforce proper peer dep declarations
strict-peer-dependencies=true

# Auto install peers to reduce friction
auto-install-peers=true

# Engine strict mode - enforce Node.js version requirements
engine-strict=true
